"use strict";

// Compatibility entrypoint. The active implementation serves the checked-out
// MonkeyGG2 files from vendor/monkeygg2 and never loads the live GitHub Pages site.
module.exports = require("./game-mode-local");

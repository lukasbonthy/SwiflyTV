{
  "name": "swifly-games",
  "version": "3.1.0",
  "description": "The original SwiflyTV interface converted into a locally served browser game catalog using MonkeyGG2 files.",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/start-game-mode.js",
    "dev": "node scripts/start-game-mode.js",
    "sync-games": "node scripts/sync-monkey-games.js",
    "update-games": "node scripts/sync-monkey-games.js --update",
    "base": "node server.js",
    "check": "node --check server.js && node --check game-mode-local.js && node --check game-mode-client.js && node --check scripts/start-game-mode.js && node --check scripts/sync-monkey-games.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "socket.io": "^4.8.1",
    "playwright-core": "1.49.1",
    "@consumet/extensions": "latest",
    "axios": "^1.0.0",
    "cheerio": "1.0.0-rc.12"
  }
}
